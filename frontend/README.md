# frontend

Placeholder. The current frontend still lives in `backend/src/assets` (server-rendered
`index.html` + Vue 2 app, embedded into the Go binary). This directory is reserved for
the frontend once it's split out of the backend as a standalone project.
